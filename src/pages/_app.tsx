import '@/styles/globals.css'
import type { AppProps } from 'next/app'
import Head from 'next/head'

export default function App({ Component, pageProps }: AppProps) {
  return (
    <>
      <Head>
        <title>MOD Dentistry - Modern Dental Care in Houston</title>
        <meta name="description" content="Experience dentistry reinvented at MOD Dentistry. Modern, comfortable, and patient-centered dental care in Houston, Texas." />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Component {...pageProps} />
    </>
  )
}